# CN-012/021: Pinned to exact versions. Update intentionally with pip-audit review.
# Host-state automation: Ansible-backed jobs (hardening, probe, prune,
# observability…) run their playbooks through ansible-runner. Both packages
# must be present in any image that executes jobs — without them every
# Ansible-backed executor fails at start.
ansible-core==2.21.2
ansible-runner==2.4.3
# CN-012: >=2.20.0 required — fixes Terrapin (CVE-2023-46445/48795) and CVE-2025-22886.
asyncssh==2.22.0
boto3==1.42.89
# CN-018: >=44.0.1 required — fixes CVE-2023-49083, CVE-2024-6119, CVE-2024-12797.
cryptography==46.0.7
# CN-023: Explicit bcrypt pin — >=4.0.1 required for correct >72-byte error handling.
# Called directly (not via passlib) because passlib 1.7.4 can't talk to bcrypt >= 5.
# passlib itself still comes in transitively through Odoo core.
bcrypt==5.0.0
PyYAML==6.0.3
