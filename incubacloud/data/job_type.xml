<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="host_probe" model="cloud.job.type">
            <field name="name">Host Probe</field>
            <field name="code">host_probe</field>
            <field name="apply_to">host</field>
            <field name="description">Probes the host to check its status and availability.</field>

        </record>

        <record id="host_metrics" model="cloud.job.type">
            <field name="name">Host Metrics</field>
            <field name="code">host_metrics</field>
            <field name="apply_to">host</field>
            <field name="description">Collects CPU cores, total RAM and disk health from the host.</field>
            <field name="priority_tier">low</field>
        </record>

        <record id="deploy_instance" model="cloud.job.type">
            <field name="name">Deploy Instance</field>
            <field name="code">deploy_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Deploys a doodba instance via copier on the target host.</field>
        </record>

        <record id="full_setup" model="cloud.job.type">
            <field name="name">Setup Host</field>
            <field name="code">full_setup</field>
            <field name="apply_to">host</field>
            <field name="description">Full host setup: checks OS/disk, installs all tools, deploys Traefik.</field>
        </record>

        <record id="push_github_webhook_edge" model="cloud.job.type">
            <field name="name">Publish GitHub Webhook Allowlist</field>
            <field name="code">push_github_webhook_edge</field>
            <field name="apply_to">host</field>
            <field name="description">Writes the Traefik dynamic config that restricts the GitHub webhook path to GitHub's published source ranges, so a forged signature is refused before its body is read.</field>
        </record>

        <record id="push_trusted_proxies" model="cloud.job.type">
            <field name="name">Push Trusted Proxy Settings</field>
            <field name="code">push_trusted_proxies</field>
            <field name="apply_to">host</field>
            <field name="description">Re-ships the Traefik configuration that says which proxies in front of this host may be believed, and restarts the proxy so the static setting takes effect.</field>
            <!-- Shown in the host action bar: applying a change to the
                 proxy settings otherwise needs a full setup run, which
                 reinstalls tooling this has nothing to do with. -->
            <field name="show_as_action">True</field>
            <field name="action_icon">fa-shield</field>
            <field name="action_order">40</field>
            <!-- The last step restarts Traefik, and on the host that
                 serves this panel that means the panel's own requests
                 and its bus connection die for a few seconds. Without
                 this notice the operator reads the resulting
                 "connection interrupted" as a broken action. -->
            <field name="action_confirm">Traefik will restart on this host. The panel and every instance it serves will be unreachable for a few seconds, and this page may report a lost connection while it comes back.</field>
        </record>

        <record id="start_instance" model="cloud.job.type">
            <field name="name">Start Instance</field>
            <field name="code">start_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Starts a stopped doodba instance (docker compose up -d).</field>
        </record>

        <record id="stop_instance" model="cloud.job.type">
            <field name="name">Stop Instance</field>
            <field name="code">stop_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Stops a running doodba instance (docker compose stop).</field>
        </record>

        <record id="restart_instance" model="cloud.job.type">
            <field name="name">Restart Instance</field>
            <field name="code">restart_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Restarts a doodba instance (docker compose restart).</field>
        </record>

        <record id="rebuild_instance" model="cloud.job.type">
            <field name="name">Rebuild Instance</field>
            <field name="code">rebuild_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Rebuilds the Odoo Docker image without cache (docker compose build --pull --no-cache odoo).</field>
        </record>

        <record id="delete_project" model="cloud.job.type">
            <field name="name">Delete Project</field>
            <field name="code">delete_project</field>
            <field name="apply_to">host</field>
            <field name="description">Removes the project directory from the host after verifying no containers remain under it.</field>
        </record>

        <record id="delete_instance" model="cloud.job.type">
            <field name="name">Delete Instance</field>
            <field name="code">delete_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Stops and removes a doodba instance from the host.</field>
        </record>

        <record id="purge_archived_backups" model="cloud.job.type">
            <field name="name">Delete Archived Copy</field>
            <field name="code">purge_archived_backups</field>
            <field name="apply_to">instance</field>
            <field name="description">Empties an archived instance's backup prefix from a throwaway container, then removes the record. Nothing is redeployed.</field>
        </record>

        <record id="restore_instance" model="cloud.job.type">
            <field name="name">Restore Database</field>
            <field name="code">restore_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Restores a doodba instance database from a .zip backup file.</field>
        </record>

        <record id="grant_restore_upload_key" model="cloud.job.type">
            <field name="name">Grant Restore Upload Key</field>
            <field name="code">grant_restore_upload_key</field>
            <field name="apply_to">instance</field>
            <field name="description">Installs a one-use SSH key on the host, restricted to writing one directory, so a backup archive can be uploaded without anybody being given access to the machine.</field>
        </record>

        <record id="verify_restore_upload" model="cloud.job.type">
            <field name="name">Check Uploaded Archive</field>
            <field name="code">verify_restore_upload</field>
            <field name="apply_to">instance</field>
            <field name="description">Reports the name, size and SHA-256 of what arrived through an upload key, computed on the host, so it can be confirmed before it is restored.</field>
        </record>

        <record id="revoke_restore_upload_key" model="cloud.job.type">
            <field name="name">Revoke Restore Upload Key</field>
            <field name="code">revoke_restore_upload_key</field>
            <field name="apply_to">instance</field>
            <field name="description">Removes a one-use upload key from the host, and its directory unless a restore is about to read it.</field>
        </record>

        <record id="move_cutover" model="cloud.job.type">
            <field name="name">Move Cutover</field>
            <field name="code">move_cutover</field>
            <field name="apply_to">instance</field>
            <field name="description">Verifies the destination instance is healthy and cuts the instance over to the new host (flips host_id, re-points DNS).</field>
        </record>

        <record id="move_cleanup_source" model="cloud.job.type">
            <field name="name">Move Cleanup (source)</field>
            <field name="code">move_cleanup_source</field>
            <field name="apply_to">instance</field>
            <field name="description">Removes the old host copy after a successful move, without archiving the instance record.</field>
        </record>

        <record id="move_rollback_cleanup" model="cloud.job.type">
            <field name="name">Move Rollback Cleanup</field>
            <field name="code">move_rollback_cleanup</field>
            <field name="apply_to">instance</field>
            <field name="description">Cleans up the half-built destination after a rolled-back move (docker compose down -v + rm -rf, non-fatal).</field>
        </record>

        <record id="export_instance" model="cloud.job.type">
            <field name="name">Export for Development</field>
            <field name="code">export_instance</field>
            <field name="apply_to">instance</field>
            <field name="description">Packages the instance folder as a tarball (respecting .gitignore) and makes it available for download.</field>
        </record>

        <record id="setup_whitelist" model="cloud.job.type">
            <field name="name">Setup Whitelist</field>
            <field name="code">setup_whitelist</field>
            <field name="apply_to">host</field>
            <field name="description">Creates or updates ~/globalwhitelist/docker-compose.yaml and brings whitelist proxy services up.</field>
        </record>

        <record id="install_observability" model="cloud.job.type">
            <field name="name">Install Observability</field>
            <field name="code">install_observability</field>
            <field name="apply_to">host</field>
            <field name="description">Installs the metrics agents (node_exporter, cAdvisor, vmagent) on the host and labels every container with its instance/project so the panel can attribute metrics. Push-only: no inbound port is opened.</field>
            <field name="priority_tier">low</field>
            <!-- Deliberately NOT offered as a button. It used to be one,
                 justified as "how an operator refreshes the labels after
                 adding instances" — a task that became automatic when
                 deploy/move/remove started refreshing them itself. What
                 was left was a button whose only remaining purpose was
                 enrolment, and enrolment is not a decision: there is no
                 host to which observability does not apply. It is now
                 applied by the reconciliation cron, so the button was
                 pure noise, and worse, its presence implied a step that
                 an operator could forget. The job itself stays — it is
                 what the cron queues. -->
            <field name="show_as_action">False</field>
        </record>

        <record id="deploy_metrics_central" model="cloud.job.type">
            <field name="name">Deploy Metrics Central</field>
            <field name="code">deploy_metrics_central</field>
            <field name="apply_to">host</field>
            <field name="description">Deploys the central metrics stack (VictoriaMetrics + Grafana with provisioned dashboards) on the target host. Run it against the panel host to co-locate, or against a dedicated VPS to move it.</field>
        </record>

        <record id="sync_metrics_accounts" model="cloud.job.type">
            <field name="name">Sync Metrics Accounts</field>
            <field name="code">sync_metrics_accounts</field>
            <field name="apply_to">host</field>
            <!-- Deliberately NOT in cloud.job._hidden_job_types: hidden
                 types are exempt from the active-job guard, and this one
                 rewrites the very file a central deployment writes. Being
                 visible is what serialises the two against the same
                 host. -->
            <field name="show_as_action">False</field>
            <field name="description">Rewrites the gateway's access-control list on an already-deployed central and provisions the Grafana organisations of any new accounts. Granting an account is a sub-second reload of the proxy — it never touches the compose file, so it cannot recreate VictoriaMetrics, Loki or Grafana.</field>
        </record>

        <record id="host_hardening" model="cloud.job.type">
            <field name="name">Harden Host</field>
            <field name="code">host_hardening</field>
            <field name="apply_to">host</field>
            <field name="description">Applies the in-host security recipe (non-root operator user, hardened sshd on a rotated port, nftables allowlist, fail2ban, sysctl, unattended-upgrades, docker daemon, journald, auditd) and disables root SSH only after proving the new login works.</field>
        </record>

        <record id="delete_host" model="cloud.job.type">
            <field name="name">Delete Host</field>
            <field name="code">delete_host</field>
            <field name="apply_to">host</field>
            <field name="description">Stops Traefik, removes the traefik directory and archives the host record.</field>
        </record>

        <record id="docker_prune" model="cloud.job.type">
            <field name="name">Docker Prune</field>
            <field name="code">docker_prune</field>
            <field name="apply_to">host</field>
            <field name="description">Reclaims disk space by removing unused Docker containers, images, networks and build cache (docker system prune -af).</field>
            <field name="priority_tier">low</field>
        </record>

        <record id="backup_list" model="cloud.job.type">
            <field name="name">List Backups</field>
            <field name="code">backup_list</field>
            <field name="apply_to">instance</field>
            <field name="description">Lists available backups via duplicity collection-status.</field>
        </record>

        <record id="backup_create" model="cloud.job.type">
            <field name="name">Create Backup</field>
            <field name="code">backup_create</field>
            <field name="apply_to">instance</field>
            <field name="description">Creates a manual backup (duplicity for production, click-odoo-backupdb for others).</field>
        </record>

        <record id="backup_download" model="cloud.job.type">
            <field name="name">Download Backup</field>
            <field name="code">backup_download</field>
            <field name="apply_to">instance</field>
            <field name="description">Downloads a specific backup from duplicity as a ZIP file.</field>
        </record>

        <record id="backup_download_neutralized" model="cloud.job.type">
            <field name="name">Download Neutralized Backup</field>
            <field name="code">backup_download_neutralized</field>
            <field name="apply_to">instance</field>
            <field name="description">Downloads a neutralized copy of a backup (crons off, mail servers archived, credentials scrubbed, database.is_neutralized=true). Safe for testing/troubleshooting.</field>
        </record>

        <record id="backup_restore" model="cloud.job.type">
            <field name="name">Restore Backup</field>
            <field name="code">backup_restore</field>
            <field name="apply_to">instance</field>
            <field name="description">Restores a production instance from a duplicity backup (destructive operation).</field>
        </record>

        <record id="instance_health" model="cloud.job.type">
            <field name="name">Instance Health Check</field>
            <field name="code">instance_health</field>
            <field name="apply_to">instance</field>
            <field name="description">Checks container state, HTTP health, CPU/memory peaks and error log count for a deployed instance.</field>
            <field name="priority_tier">low</field>
        </record>

    </data>
</odoo>
