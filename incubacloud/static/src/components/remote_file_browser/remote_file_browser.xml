<templates>

    <t t-name="incubacloud.RemoteFileBrowser">
        <div class="ic-modal-overlay" t-on-click.self="props.onCancel">
            <div class="ic-modal ic-modal--filebrowser"
                 role="dialog"
                 aria-modal="true"
                 aria-labelledby="ic-remote-browser-title"
                 t-ref="box"
                 t-on-click.stop="">

                <header class="ic-modal__header">
                    <h3 id="ic-remote-browser-title">Import Instance from Host</h3>
                    <button t-if="state.step !== 'importing'"
                            class="ic-modal__close" aria-label="Close" t-on-click="props.onCancel">
                        <t t-out="env.icon('x', 16)"/>
                    </button>
                </header>

                <!-- ── Browse step ────────────────────────────────── -->
                <t t-if="state.step === 'browse'">
                    <section class="ic-modal__body">

                        <!-- Breadcrumb -->
                        <div class="ic-fb-breadcrumb">
                            <t t-foreach="breadcrumbs" t-as="bc" t-key="bc.path">
                                <span class="ic-fb-bc-item"
                                      t-on-click="() => this.onClickBreadcrumb(bc.path)"
                                      t-esc="bc.name"/>
                                <t t-if="!bc_last">
                                    <span class="ic-fb-bc-sep">/</span>
                                </t>
                            </t>
                        </div>

                        <t t-if="state.loading">
                            <div class="ic-fb-loading">
                                <span class="rl-spin"/>
                                <span>Loading directory…</span>
                            </div>
                        </t>

                        <t t-elif="state.error">
                            <div class="ic-fb-alert" role="alert">
                                <span class="ic-fb-alert__icon"><t t-out="env.icon('alert', 15)"/></span>
                                <span t-esc="state.error"/>
                            </div>
                        </t>

                        <t t-else="">
                            <!-- Current dir is doodba badge -->
                            <t t-if="state.isDoodba">
                                <div class="ic-fb-doodba-badge">
                                    <span class="ic-fb-doodba-icon"><t t-out="env.icon('check', 15)"/></span>
                                    <span>Doodba instance detected</span>
                                    <t t-if="state.copierInfo.project_name">
                                        <span class="ic-fb-doodba-name">— <strong t-esc="state.copierInfo.project_name"/></span>
                                    </t>
                                    <t t-if="state.copierInfo.odoo_version">
                                        <span class="ic-fb-doodba-ver">(Odoo <t t-esc="state.copierInfo.odoo_version"/>)</span>
                                    </t>
                                </div>
                            </t>

                            <!-- Directory listing -->
                            <div class="ic-fb-list">
                                <!-- Parent dir -->
                                <t t-if="state.parentPath">
                                    <div class="rl-row click ic-fb-row" t-on-click="goUp">
                                        <div class="rl-rico"><i class="fa fa-level-up"/></div>
                                        <div class="rl-rname">..</div>
                                    </div>
                                </t>

                                <t t-foreach="state.entries" t-as="entry" t-key="entry.name">
                                    <div t-att-class="'rl-row click ic-fb-row'
                                         + (entry.is_doodba ? ' ic-fb-row-doodba' : '')
                                         + (entry.already_imported ? ' ic-fb-row-imported' : '')"
                                         t-on-click="() => this.onClickDir(entry)">
                                        <div class="rl-rico">
                                            <i t-att-class="entry.is_doodba ? 'fa fa-cube' : 'fa fa-folder'"/>
                                        </div>
                                        <div class="rl-rname" t-esc="entry.name"/>
                                        <div class="rl-rspacer"/>
                                        <t t-if="entry.is_doodba">
                                            <span class="ic-fb-badge ic-fb-badge-doodba">doodba</span>
                                        </t>
                                        <t t-if="entry.already_imported">
                                            <span class="ic-fb-badge ic-fb-badge-imported">imported</span>
                                        </t>
                                    </div>
                                </t>

                                <t t-if="!state.entries.length and !state.parentPath">
                                    <div class="ic-fb-empty">Empty directory</div>
                                </t>
                            </div>
                        </t>
                    </section>

                    <footer class="ic-modal__footer ic-modal__footer--between">
                        <div class="ic-fb-path-display">
                            <code t-esc="state.currentPath"/>
                        </div>
                        <div class="ic-fb-footer-actions">
                            <button class="ic-btn ic-btn--ghost" t-on-click="props.onCancel">
                                Cancel
                            </button>
                            <button class="ic-btn ic-btn--primary"
                                    t-att-disabled="!state.isDoodba or state.alreadyImported"
                                    t-on-click="selectCurrent">
                                <t t-if="state.alreadyImported">Already imported</t>
                                <t t-else="">Select</t>
                            </button>
                        </div>
                    </footer>
                </t>

                <!-- ── Confirm step ───────────────────────────────── -->
                <t t-if="state.step === 'confirm'">
                    <section class="ic-modal__body">
                        <div class="ic-fb-confirm">
                            <div class="ic-fb-confirm-row">
                                <span class="ic-fb-confirm-label">Path</span>
                                <code class="ic-fb-confirm-val" t-esc="state.currentPath"/>
                            </div>
                            <t t-if="state.copierInfo.project_name">
                                <div class="ic-fb-confirm-row">
                                    <span class="ic-fb-confirm-label">Project</span>
                                    <span class="ic-fb-confirm-val" t-esc="state.copierInfo.project_name"/>
                                </div>
                            </t>
                            <t t-if="state.copierInfo.odoo_version">
                                <div class="ic-fb-confirm-row">
                                    <span class="ic-fb-confirm-label">Odoo Version</span>
                                    <span class="ic-fb-confirm-val" t-esc="state.copierInfo.odoo_version"/>
                                </div>
                            </t>
                        </div>
                        <p class="ic-fb-confirm-hint">
                            This will create a project and instance in IncubaCloud mapped to
                            this directory. The running instance will not be affected.
                        </p>
                    </section>
                    <footer class="ic-modal__footer ic-modal__footer--between">
                        <button class="ic-btn ic-btn--ghost" t-on-click="backToBrowse">
                            <t t-out="env.icon('back', 15)"/>
                            Back
                        </button>
                        <button class="ic-btn ic-btn--primary" t-on-click="doImport">
                            <t t-out="env.icon('download', 15)"/>
                            Import
                        </button>
                    </footer>
                </t>

                <!-- ── Importing step ─────────────────────────────── -->
                <t t-if="state.step === 'importing'">
                    <section class="ic-modal__body">
                        <div class="ic-fb-steps">
                            <t t-foreach="state.messages" t-as="msg" t-key="msg_index">
                                <div class="rl-jstep done">
                                    <span class="rl-si2"><t t-out="env.icon('check', 15)"/></span>
                                    <span t-esc="msg.text"/>
                                </div>
                            </t>
                            <div class="rl-jstep run">
                                <span class="rl-si2"><span class="rl-spin"/></span>
                                <span>Importing instance…</span>
                            </div>
                        </div>
                    </section>
                    <footer class="ic-modal__footer"/>
                </t>

                <!-- ── Done step ──────────────────────────────────── -->
                <t t-if="state.step === 'done'">
                    <section class="ic-modal__body">
                        <div class="ic-fb-steps">
                            <t t-foreach="state.messages" t-as="msg" t-key="msg_index">
                                <div class="rl-jstep done">
                                    <span class="rl-si2"><t t-out="env.icon('check', 15)"/></span>
                                    <span t-esc="msg.text"/>
                                </div>
                            </t>
                        </div>
                    </section>
                    <footer class="ic-modal__footer">
                        <button class="ic-btn ic-btn--primary" t-on-click="goToInstance">
                            <t t-out="env.icon('fwd', 15)"/>
                            Go to instance
                        </button>
                    </footer>
                </t>

                <!-- ── Error step ─────────────────────────────────── -->
                <t t-if="state.step === 'error'">
                    <section class="ic-modal__body">
                        <div class="ic-fb-steps">
                            <t t-foreach="state.messages" t-as="msg" t-key="msg_index">
                                <div class="rl-jstep done">
                                    <span class="rl-si2"><t t-out="env.icon('check', 15)"/></span>
                                    <span t-esc="msg.text"/>
                                </div>
                            </t>
                        </div>
                        <div class="ic-fb-alert" role="alert">
                            <span class="ic-fb-alert__icon"><t t-out="env.icon('alert', 15)"/></span>
                            <span t-esc="state.error"/>
                        </div>
                    </section>
                    <footer class="ic-modal__footer ic-modal__footer--between">
                        <button class="ic-btn ic-btn--ghost" t-on-click="retry">
                            <t t-out="env.icon('back', 15)"/>
                            Try again
                        </button>
                        <button class="ic-btn ic-btn--ghost" t-on-click="props.onCancel">Close</button>
                    </footer>
                </t>

            </div>
        </div>
    </t>

</templates>
